input LoginInput {
  email: String!
  password: String!
}

mutation Login($data: LoginInput!) {
  login(data: $data) {
    token
    isCompany
  }
}

query GetMyAccount {
  getMyAccount {
    id
    accountId
    displayName
    profileImage
  }
}
